
// This shows a simple example of how to archive the build output artifacts.
pipeline {
        agent any
        tools {nodejs "NODEJS"}


stages {
        stage('install') {
            steps {
                 sh "npm install --force"
            }
        }
         stage("Build"){
           steps{
                sh "npm run ng serve --port 8095"
           }
        }
    }
}
